{
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "nodemon": "^3.0.1",
    "passport": "^0.6.0",
    "pg": "^8.11.3"
  },
  "scripts": {
    "start": "nodemon -r dotenv/config server.js",
    "deploy": "node server.js",
    "build": "cd front_end && npm i && npm run build"
  }
}